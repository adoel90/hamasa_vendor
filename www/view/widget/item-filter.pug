form.form
  .form-group  
    label.form-label Cari Berdasarkan
    .row
      .col-lg-4.col-md-4.col-sm-4.col-xs-12
        ui-select(ng-model="secondSelectedFilter.item", style="margin-right: 15px")
          ui-select-match
            span(ng-bind="$select.selected.name")
          ui-select-choices(repeat="secondFilter in (secondSearchFilters | filter: $select.search) track by $index")
            span(ng-bind="secondFilter.name")
      .col-lg-5.col-md-5.col-sm-5.col-xs-12
        .input-group
          input.form-input(type="text", ng-model="secondSearchVal", placeholder="{{secondSelectedFilter.item.placeholder}}")

    .row.m-top-15
      .col-lg-4.col-md-4.col-sm-4.col-xs-12
        ui-select(ng-model="firstSelectedFilter.item", style="margin-right: 15px")
          ui-select-match
            span(ng-bind="$select.selected.name")
          ui-select-choices(repeat="firstFilter in (firstSearchFilters | filter: $select.search) track by $index")
            span(ng-bind="firstFilter.name")
      .col-lg-5.col-md-5.col-sm-5.col-xs-12
        .input-group
          input.form-input(type="text", ng-model="firstSearchVal", placeholder="{{firstSelectedFilter.item.placeholder}}", autofocus)
      .col-lg-3.col-md-3.col-sm-3s.col-xs-12
        button.button.button--secondary.button--base(ng-click="doSearchFilter(firstSearchVal, firstSelectedFilter.item.by, secondSearchVal, secondSelectedFilter.item.by)") Cari